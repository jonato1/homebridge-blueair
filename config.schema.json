{
  "pluginAlias": "BlueAir",
  "pluginType": "platform",
  "singular": true,
  "headerDisplay": "BlueAir Devices are exposed following login to the BlueAir API. All devices on the account should be listed in the Home app following successful login.",
  "footerDisplay": "",
  "schema": {
    "type": "object",
    "properties": {
        "username": {
            "title": "username",
            "type": "string",
            "required": true
        },
        "password": {
            "title": "password",
            "type": "string",
            "required": true
        },
        "enableAWS": {
            "title": "Enable AWS API connection (only for debugging currently)",
            "type": "boolean",
            "required": false
        },
        "options": {
            "title": "Feature Options",
            "type": "array",

            "items": {
                "type": "string",
                "title": "Feature Option",
                "required": false,
                "description": "Enter only one option per entry. See project page for the complete list of available options.",
                "placeholder": "e.g. Disable.uuid"
            }
        }
    }
  }
}
